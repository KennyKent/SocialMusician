<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.suonk.socialmusician">
    <!-- To auto-complete the email text field in the sign_in form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_musical_notes_symbols"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name="com.suonk.socialmusician.controller.activity.SignUpActivity"/>
        <activity android:name="com.suonk.socialmusician.controller.activity.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.suonk.socialmusician.controller.activity.SignInActivity" />
        <activity
            android:name="com.suonk.socialmusician.controller.activity.MainActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name="com.suonk.socialmusician.controller.activity.MyProfileActivity"
            android:theme="@style/AppTheme" />
    </application>

</manifest>